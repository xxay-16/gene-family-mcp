# PlantCARE 提交与邮件轮询脚本使用文档

本文档说明如何使用以下脚本：

- `plantcare_submit.py`：仅提交 PlantCARE 任务
- `test.py`：提交任务 + 每 10 秒轮询邮箱 + 命中后下载附件

## 1. 环境准备

- Python 3.9 及以上
- 可访问：
  - `https://bioinformatics.psb.ugent.be/webtools/plantcare/cgi-bin/CallMat_onCluster.htpl`
  - QQ 邮箱 IMAP 服务器 `imap.qq.com:993`
- 输入文件建议为纯文本 FASTA（示例：`test.fa`）

## 2. 文件说明

- `plantcare_submit.py`
  - 作用：构造 multipart/form-data 并向 PlantCARE 提交
- `test.py`
  - 作用：自动生成随机 ref，提交任务，轮询邮件，按 ref 定位回邮并下载附件

## 3. 快速开始（推荐）

在项目目录执行：

```bash
python test.py --email 你的邮箱@qq.com --password 你的授权码 --imap-host imap.qq.com --poll-interval 10 --max-polls 360
```

执行过程：

1. 自动生成随机 `ref`（例如 `wd93rojubt9j`）
2. 提交 PlantCARE 任务（`Field_REF=随机ref`）
3. 每 10 秒轮询一次邮箱
4. 用 `ref` 在邮件正文/标题定位目标邮件
5. 下载附件到目录：`plantcare_attachments_<ref>`

## 4. 常用命令

### 4.1 仅提交（不轮询邮箱）

```bash
python plantcare_submit.py --email 你的邮箱@qq.com --ref myref123 --file test.fa
```

或直接传序列：

```bash
python plantcare_submit.py --email 你的邮箱@qq.com --ref myref123 --sequence ACTGACTGACTG
```

### 4.2 提交 + 轮询 + 下载附件（默认随机 ref）

```bash
python test.py --email 你的邮箱@qq.com --password 你的授权码 --imap-host imap.qq.com
```

### 4.3 指定 ref（一般不需要）

```bash
python test.py --email 你的邮箱@qq.com --password 你的授权码 --imap-host imap.qq.com --ref abc123xyz
```

### 4.4 指定附件保存目录

```bash
python test.py --email 你的邮箱@qq.com --password 你的授权码 --imap-host imap.qq.com --save-attachments-dir D:\Desktop\gf\downloads
```

## 5. `test.py` 参数说明

- `--email`：邮箱地址（必填）
- `--password`：邮箱授权码（必填；也可用环境变量 `MAIL_PASSWORD`）
- `--imap-host`：IMAP 服务器（QQ 为 `imap.qq.com`）
- `--port`：IMAP SSL 端口，默认 `993`
- `--folder`：邮箱文件夹，默认 `INBOX`
- `--submit-url`：PlantCARE 提交地址
- `--ref`：任务标识；留空则自动随机生成
- `--sequence`：直接提交序列
- `--file`：提交文件，默认 `test.fa`
- `--submit-timeout`：提交流程超时秒数
- `--poll-interval`：轮询间隔秒数，默认 `10`
- `--max-polls`：最多轮询次数，默认 `60`
- `--save-attachments-dir`：附件保存目录

说明：`--sequence` 和 `--file` 至少提供一个。

## 6. 返回信息解读

- 提交成功常见提示：
  - `has been submitted ... will be emailed to ...`
- 轮询状态：
  - `第N轮: 未搜索到包含ref的新邮件`
  - `第N轮: 未发现正文或标题包含ref的邮件`
  - 命中后会打印 `UID/From/Date/Subject` 并列出下载附件路径

## 7. 常见问题

- 邮箱写成 `qq.con` 会登录失败，应为 `qq.com`
- 若一直收不到回邮：
  - 增大 `--max-polls`（如 360=约1小时）
  - 检查垃圾邮件箱
  - 检查是否开启 IMAP 并使用正确授权码
- PlantCARE 页面提示邮箱非法：
  - 检查 `--email` 拼写

## 8. 安全建议

- 使用邮箱授权码，不要使用邮箱登录密码
- 授权码泄露后应立即在邮箱后台重置



Sign up to the console is restricted. You can contact an administrator to update console sign up restrictions by setting _APP_CONSOLE_WHITELIST_ROOT to "disabled".